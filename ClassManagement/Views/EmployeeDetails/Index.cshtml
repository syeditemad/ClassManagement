@model ClassManagement.BAL.MultipleTable_Bind

@{
    ViewBag.Title = "Index";
}
@if (TempData["Success"] != null)
{
    <div class="alert alert-success">
        @TempData["Success"];
    </div>
}
<div class="row">

    <div class="col-md-12">
        @{await Html.RenderPartialAsync("GetEmployeeDetailsList", Model.GetEmployeeDetailsList);}
        <hr />
        @{await Html.RenderPartialAsync("NomineeDetailsList", Model.NomineeDetailsList);}
        <hr />
        @{await Html.RenderPartialAsync("GetCountryListView", Model.GetCountryListView);}

        <hr />
        @{await Html.RenderPartialAsync("GetCountryStateViewList", Model.GetCountryStateViewList);}

        @*@{await Html.RenderPartialAsync("_CorrespondenceAddress", Model.aadhaarDetails.ObjCorrespondenceAddress);}
            <hr />
            @{await Html.RenderPartialAsync("_PermanentAddress", Model.aadhaarDetails.ObjPermanentAddress);}
            <hr />*@
        @*@foreach (var bankModel in Model.lstBankDetails.DefaultIfEmpty())
                {
                    await Html.RenderPartialAsync("_BankDetails", bankModel);
                }
                @foreach (var dematModel in Model.lstDematDetails.DefaultIfEmpty())
                {
                    await Html.RenderPartialAsync("_DematDetails", dematModel);
                }
            </div>*@
    </div>
</div>
<script type="text/javascript"></script>
<script>
    $(document).ready(function () {
        window.history.forward();
        function onBack() {
            window.history.forward();
        }
    });
   
</script>